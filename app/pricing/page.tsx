import { Card, LinkButton } from "@/components/ui";
import { PLANS } from "@/lib/billing";
export default function PricingPage() { return <main className="min-h-screen px-6 py-12"><div className="mx-auto max-w-5xl"><a href="/" className="text-sm font-semibold text-blue-700">Home</a><h1 className="mt-8 text-4xl font-black text-slate-900">Simple plans for teams and clubs</h1><p className="mt-3 text-slate-600">Start free, then upgrade when your team or league needs more groups, FAQs, and message volume.</p><div className="mt-8 grid gap-5 md:grid-cols-3">{Object.values(PLANS).map((plan) => <Card key={plan.id} className="flex flex-col"><h2 className="text-2xl font-bold">{plan.name}</h2><p className="mt-2 text-3xl font-black text-blue-800">{plan.monthlyPrice}</p><ul className="mt-5 flex-1 space-y-2 text-sm text-slate-600">{plan.features.map((feature) => <li key={feature}>✓ {feature}</li>)}</ul><LinkButton href="/signup" className="mt-6">Choose {plan.name}</LinkButton></Card>)}</div></div></main>; }
