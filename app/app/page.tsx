import Link from "next/link";
import { Card, EmptyState, Field, Button, Badge } from "@/components/ui";
import { requireUser } from "@/lib/auth";
import { createOrganization } from "@/lib/actions";
export default async function DashboardPage() { const { supabase, user } = await requireUser(); const { data: memberships } = await supabase.from("organization_members").select("*").eq("user_id", user.id); if (!memberships?.length) return <Onboarding />; const { data: org } = await supabase.from("organizations").select("*").eq("id", memberships[0].organization_id).single(); if (!org) return <Onboarding />; const { data: teams } = await supabase.from("teams").select("*").eq("organization_id", org.id).eq("is_active", true).order("created_at"); const { data: connections } = await supabase.from("groupme_connections").select("team_id").eq("organization_id", org.id).eq("is_active", true); const connected = new Set((connections || []).map((c) => c.team_id)); return <div><div className="flex flex-wrap items-center justify-between gap-4"><div><h1 className="text-3xl font-black text-slate-900">{org.name}</h1><p className="mt-1 text-slate-600">Manage your team bots, games, FAQs, and reminders.</p></div><Badge>{org.plan} plan</Badge></div><div className="mt-8 grid gap-5 md:grid-cols-3">{teams?.map((team) => <Card key={team.id}><h2 className="text-xl font-bold">{team.name}</h2><p className="mt-1 text-sm text-slate-500">{[team.age_group, team.season].filter(Boolean).join(" • ") || "Team"}</p><div className="mt-4 flex gap-2"><Badge tone={connected.has(team.id) ? "green" : "amber"}>{connected.has(team.id) ? "GroupMe connected" : "Connect GroupMe"}</Badge></div><Link href={`/app/team/${team.id}`} className="mt-5 inline-block text-sm font-semibold text-blue-700">Open team</Link></Card>)}</div></div>; }
function Onboarding() { return <div className="mx-auto max-w-2xl"><EmptyState title="Create your first organization" body="Set up a tenant and team before connecting GroupMe or publishing game details." /><Card className="mt-6"><form action={createOrganization} className="grid gap-4"><Field label="Organization name" name="organizationName" placeholder="Northside Baseball" required /><Field label="Team name" name="teamName" placeholder="12U Blue" required /><Field label="Age group" name="ageGroup" placeholder="12U" /><Field label="Season" name="season" placeholder="Spring 2026" /><Button>Create organization</Button></form></Card></div>; }
