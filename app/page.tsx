import { Bot, CalendarDays, MessageCircle, ShieldCheck } from "lucide-react";
import { LinkButton, Card } from "@/components/ui";
import { PLANS } from "@/lib/billing";
const features = [ { icon: MessageCircle, title: "Answers in GroupMe", body: "Parents ask naturally and get instant answers from your team's own game data." }, { icon: CalendarDays, title: "Game logistics hub", body: "Time, arrival, uniforms, field, maps, parking, notes, and FAQs stay organized." }, { icon: ShieldCheck, title: "Multi-tenant by design", body: "Every organization, team, bot, and log is tenant-scoped with Supabase RLS." }, { icon: Bot, title: "Coach-safe AI", body: "The assistant uses stored facts only and flags missing details for follow-up." } ];
export default function LandingPage() { return <main className="min-h-screen bg-gradient-to-b from-blue-950 via-blue-900 to-slate-100 text-white"><nav className="mx-auto flex max-w-6xl items-center justify-between px-6 py-6"><div className="text-xl font-black">Ask Coach</div><div className="flex gap-3 text-sm"><a href="/pricing" className="py-2 text-blue-100">Pricing</a><LinkButton href="/login" className="bg-white text-blue-900 hover:bg-blue-50">Log in</LinkButton></div></nav><section className="mx-auto max-w-6xl px-6 py-20"><div className="max-w-3xl"><p className="font-semibold text-blue-200">Built first for youth baseball. Ready for every team.</p><h1 className="mt-4 text-5xl font-black tracking-tight md:text-7xl">Stop answering the same game-day questions.</h1><p className="mt-6 text-xl text-blue-100">Ask Coach connects a team dashboard to GroupMe so parents get fast, accurate answers about time, location, uniforms, parking, and what to bring.</p><div className="mt-8 flex flex-wrap gap-3"><LinkButton href="/signup" className="bg-white text-blue-900 hover:bg-blue-50">Start free</LinkButton><LinkButton href="/pricing" className="bg-blue-700 hover:bg-blue-600">View plans</LinkButton></div></div></section><section className="bg-slate-100 px-6 py-16 text-slate-900"><div className="mx-auto grid max-w-6xl gap-5 md:grid-cols-4">{features.map((f) => <Card key={f.title}><f.icon className="h-8 w-8 text-blue-700" /><h3 className="mt-4 font-bold">{f.title}</h3><p className="mt-2 text-sm text-slate-600">{f.body}</p></Card>)}</div><div className="mx-auto mt-12 grid max-w-6xl gap-5 md:grid-cols-3">{Object.values(PLANS).map((plan) => <Card key={plan.id}><h3 className="text-xl font-bold">{plan.name}</h3><p className="mt-2 text-3xl font-black">{plan.monthlyPrice}<span className="text-sm font-medium text-slate-500">/mo</span></p><ul className="mt-4 space-y-2 text-sm text-slate-600">{plan.features.map((feature) => <li key={feature}>- {feature}</li>)}</ul></Card>)}</div></section></main>; }
